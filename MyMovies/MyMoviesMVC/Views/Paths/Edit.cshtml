@model MyMoviesMVC.Models.MoviePath

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>MoviePath</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.Movies_ID)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Movies_ID)
            @Html.ValidationMessageFor(model => model.Movies_ID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.path)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.path)
            @Html.ValidationMessageFor(model => model.path)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
